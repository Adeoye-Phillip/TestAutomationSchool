// EXPRESSED FUNCTIONS
// Also known as Anonymous Functions
// Can be called by the name of the variable assigned to it
// This function cannot be accessed before the initialization of the variable, unlike Function Declaration.


const myGreet = function (name) {
    console.log('Good morning, ' + name);
};

myGreet('Testify');