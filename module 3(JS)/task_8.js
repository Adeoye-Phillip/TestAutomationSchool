// Task 8
// Write a JavaScript program that prints out all the odd numbers between 1 and 20. Your code must use the For-loop

for (let number = 1; number <=20; number += 2) {
        console.log(number + ' is an odd number✅')
}

// The program above initializes a loop counter 'number'
// In each iteration, it prints the current value of 'number'
// Increments by 2 ensures that only odd numbers are considered.